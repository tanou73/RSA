#include <iostream>
#include <gmpxx.h>

using namespace std;

int main( int argc, char ** argv )
{
	/*
	clog << "** DECHIFFRE **" << endl;

  	string fileName = "~/.my_rsa";
	if ( argc == 2)
	{
		fileName = argv[1];
	}

	if (ifstream fichier(fileName, ios::in))
	{

	}
	else
	{
		cerr << "Impossible d'ouvrir " << fileName << endl;
		exit(1);
	}
*/
}
